# Auto-provision Prometheus datasource (reachable via traefik_net as prometheus:9090)
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
